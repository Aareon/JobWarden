# Create dummy secret key so we can use sessions
SECRET_KEY = "123456790"

# Create in-memory database
DATABASE_FILE = "jobwarden.sqlite"

# Token to be used by employees on the registration page
ACCESS_TOKEN = "apple"
